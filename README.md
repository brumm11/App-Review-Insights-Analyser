# Weekly Product Review Pulse

Phase 0 foundation scaffold for the weekly review pulse agent.

## Quickstart

1. Install dependencies:
   - `uv sync --extra dev`
2. Initialize DB:
   - `uv run pulse init-db`
3. Explore CLI:
   - `uv run pulse --help`
