# Phase 0 Evaluations

- `uv run pulse --help` shows all expected subcommands.
- `uv run pulse init-db` creates `products`, `reviews`, `review_embeddings`, `runs`, and `themes`.
- CI (`.github/workflows/ci.yml`) runs lint + typecheck + smoke tests.
