# Phase 6 Edge Cases

- Without Docs deep link, placeholder stays in email body (safe for dry-run).
- Missing email artifacts blocks publish.
- Dry-run mode creates draft only and does not set `gmail_message_id`.
